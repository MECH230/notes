---
title: "Work Energy Theorem"
---

The Koenig decomposition for the kinetic energy of a rigid body is:
\begin{align}
    T = \frac{1}{2}m\mathbf{v}_C\cdot\mathbf{v}_C+\frac{1}{2}\mathbf{H}\cdot\bomega.
\end{align}
In this class, $\bomega = \omega \mathbf{E}_z$, $\frac{1}{2}\mathbf{H}\cdot\bomega = \frac{1}{2}I_{zz} w^2$.

In the special case of a fixed point rotation about point $O$, the kinetic energy becomes
\begin{align}
    T = \frac{1}{2}\mathbf{H}^O\cdot\bomega.
\end{align}
which simplifies to $T = \frac{1}{2}I^O_{zz} w^2$ when $\bomega = \omega\mathbf{E}_z$.

The work-energy theorem for a rigid body is:
\begin{align}
    \frac{dT}{dt} = \mathbf{F}\cdot\mathbf{v}_C+\mathbf{M}\cdot\bomega = \sum_{i=1}^N\mathbf{F}_i\cdot\mathbf{v}_i+\mathbf{M}_e\cdot\bomega.
\end{align}
Integrating the above expression with respect time between $t_A$ and $t_B$, we obtain
\begin{align}
    \begin{split}
        T_B-T_A = W_{\mathbf{F},AB}+W_{\mathbf{M},AB} = \sum_{i=1}^N W_{\mathbf{F}_i,AB}+W_{\mathbf{M}_e,AB},
        \label{eq:work-energy-theorem-RB}
    \end{split}
\end{align}
where the work of the forces and moments is calculates as follows:
\begin{align}
    \begin{split}
        W_{\mathbf{F},AB} &= \int_{t_A}^{t_B}\mathbf{F}\cdot\mathbf{v}_Cdt,\\
        W_{\mathbf{M},AB} &= \int_{t_A}^{t_B}\mathbf{M}^C\cdot\bomega dt,\\
        W_{\mathbf{F}_i,AB} &= \int_{t_A}^{t_B}\mathbf{F}_i\cdot\mathbf{v}_i dt,\\
        W_{\mathbf{M}_e,AB} &= \int_{t_A}^{t_B}\mathbf{M}_e\cdot\bomega dt,\\
    \end{split}
\end{align}
Notice that in calculating the work of a force, we dot the force with the velocity of its point of application.

As in particles and systems of particles, this theorem can be used to establish conservation of the total energy of a rigid body during a motion.

In the following section, we derive these results.

\section{Koenig's Decomposition}
By definition, the kinetic energy $T$ of a rigid body is the (continuous) sum of the kinetic energies of the differential masses composing it:
\begin{align}
    T = \frac{1}{2}\int_{\mathcal{R}}\mathbf{v}\cdot\mathbf{v}\rho dv.
\end{align}
We can introduce the $\mathbf{v}_C$ term through 
\begin{align}
    \begin{split}
        & \mathbf{v} = \mathbf{v}_C+\bomega\times\bpi,\\
        & \bpi = \mathbf{r}-\mathbf{r}_C,\\
        & \bomega = \omega_x\mathbf{e}_x+\omega_y\mathbf{e}_y+\omega_z\mathbf{e}_z.
    \end{split}
\end{align}
Substituting the above expressions in the kinetic energy integral, we get
\begin{align}
    T = \frac{1}{2}\int_{\mathcal{B}}\lp\mathbf{v}_C\cdot\mathbf{v}_C+2\mathbf{v}_C\cdot(\bomega\times\bpi)+(\bomega\times\bpi)\cdot(\bomega\times\bpi)  \rp dm
\end{align}
For the first two terms in the above integral,
\begin{align}
    \begin{split}
        & \frac{1}{2}\int_{\mathcal{B}}\mathbf{v}_C\cdot\mathbf{v}_C dm = \frac{\mathbf{v}_C\cdot\mathbf{v}_C}{2}\int dm = \frac{1}{2}m\mathbf{v}_C\cdot\mathbf{v}_C.\\
        & \int_{\mathcal{B}}\mathbf{v}_C\cdot(\bomega\times\bpi)dm = \mathbf{v}_C\cdot\lp\bomega\times\int\pi dm \rp = 0.
    \end{split}
\end{align}
Thus,
\begin{align}
    T = \frac{1}{2}m\mathbf{v}_C\cdot\mathbf{v}_C+\frac{1}{2}\int_{\mathcal{B}}(\bomega\times\bpi)\cdot(\bomega\times\bpi)dm
\end{align}
we can simplify\footnote{Here we used 
\begin{align*}
    (\mathbf{a}\times\mathbf{b})\cdot(\mathbf{c}\times\mathbf{d}) = (\mathbf{a}\cdot\mathbf{c})(\mathbf{b}\cdot\mathbf{d})-(\mathbf{a}\cdot\mathbf{d})(\mathbf{b}\cdot\mathbf{c}).
\end{align*}}
\begin{align}
    (\bomega\times\bpi)\cdot(\bomega\times\bpi) = ((\bpi\cdot\bpi)\bomega-(\bpi\cdot\bomega)\cdot\bpi).
\end{align}
Recall that
\begin{align}
    \mathbf{H}^C = \int_{\mathcal{B}}\bpi\times(\omega\times\bpi)dm = \int_{\mathcal{B}}((\bpi\cdot\bpi)\bomega-(\bpi\cdot\bomega)\bpi)dm.
\end{align}
Hence, we obtain the Koenig decomposition:
\begin{align}
    T = \frac{1}{2}m\mathbf{v}_C\cdot\mathbf{v}_C+\frac{1}{2}\mathbf{H}\cdot\bomega.
\end{align}

\subsection{Fixed Point Rotation}
In the case of fixed point rotation, $\mathbf{v}_C = \bomega\times(\mathbf{r}_C-\mathbf{r}_O)$ and $\mathbf{v}_O = 0$, then\footnote{Here we use the cyclical property of the scalar triple product: $$\mathbf{a}\cdot(\mathbf{b}\times\mathbf{c}) = \mathbf{b}\cdot(\mathbf{c}\times\mathbf{a}) = \mathbf{c}\times(\mathbf{a}\times\mathbf{b}).$$}
\begin{align}
    \begin{split}
        T &= \frac{1}{2}\mathbf{G}\cdot(\bomega\times\mathbf{r}_{C/O})+\frac{1}{2}\mathbf{H}\cdot\bomega\\
        &= \frac{1}{2}\bomega\cdot(\mathbf{r}_{C/O}\times\mathbf{G})+\frac{1}{2}\mathbf{H}^C\cdot\bomega\\
        &= \frac{1}{2}(\mathbf{r}_{C/O}\times\mathbf{G}+\mathbf{H})\cdot\bomega\\
        &= \frac{1}{2}\mathbf{H}^O\cdot\bomega.
    \end{split}
\end{align}

\section{The Work-Energy Theorem}
\begin{align}
    \dot{T} = \frac{1}{2}m\dot{\mathbf{v}}_C\cdot\mathbf{v}_C+\frac{1}{2}m\mathbf{v}_C\cdot\dot{\mathbf{v}}_C+\frac{1}{2}\dot{\mathbf{H}}\cdot\bomega+\frac{1}{2}\mathbf{H}\cdot\dot{\bomega}.
\end{align}
We need to show that $\dot{\mathbf{H}}\cdot\bomega = \mathbf{H}\cdot\dot{\bomega}$.
\begin{align}
    \begin{split}
        \balpha = \dot{\bomega} &= \frac{d}{dt}(\omega_x\mathbf{e}_x+\omega_y\mathbf{e}_y+\omega_z\mathbf{e}_z)\\
        &= \dot{\omega}_x\mathbf{e}_x+\dot{\omega}_y\mathbf{e}_y+\dot{\omega}_z\mathbf{e}_z+\omega_x\dot{\mathbf{e}}_x+\omega_y\dot{\mathbf{e}}_y+\omega_z\dot{\mathbf{e}}_z\\
        &= \dot{\omega}_x\mathbf{e}_x+\dot{\omega}_y\mathbf{e}_y+\dot{\omega}_z\mathbf{e}_z+\bomega\times(\omega_x\mathbf{e}_x+\omega_y\mathbf{e}_y+\omega_z\mathbf{e}_z)\\
        &= \dot{\omega}_x\mathbf{e}_x+\dot{\omega}_y\mathbf{e}_y+\dot{\omega}_z\mathbf{e}_z.
    \end{split}
\end{align}

Another direct calculation using this expression for $\balpha$ shows that
\begin{align}
    \begin{split}
        \mathbf{H}^C\cdot\dot{\bomega} = (I_{xx}\omega_x+I_{xy}\omega_y+I_{xz}\omega_z)\dot{\omega}_x+(I_{xy}\omega_x+I_{yy}\omega_y+I_{yz}\omega_z)\dot{\omega}_y+(I_{xz}\omega_x+I_{yz}\omega_y+I_{zz}\omega_z)\dot{\omega}_z.
    \end{split}
\end{align}

Comparing this to the corresponding expression for $\dot{H}^C\cdot\bomega$, we find that they are equal. Consequently,
\begin{align}
    \dot{T} = \frac{1}{2}m\dot{\mathbf{v}}_C\cdot\mathbf{v}_C+\frac{1}{2}m\mathbf{v}_C\cdot\dot{\mathbf{v}}_C+\frac{1}{2}\dot{\mathbf{H}}\cdot\bomega+\frac{1}{2}\dot{\mathbf{H}}\cdot\bomega.
\end{align}
This result implies that
\begin{align}
    \dot{T} = m\dot{\mathbf{v}}_C\cdot\mathbf{v}_C+\dot{\mathbf{H}}\cdot\bomega.
\end{align}
Invoking the balance of linear momentum and the balance of angular momentum, we obtain the work-energy theorem:
\begin{align}
    \dot{T} = \mathbf{F}\cdot\mathbf{v}_C+\mathbf{M}\cdot\bomega.
\end{align}
You should notice how this is a natural extension of the work-energy theorem for a single particle. We can then integrate these expressions of the work-energy theorem to get equation (\ref{eq:work-energy-theorem-RB}).

\section{An alternative form of the work-energy theorem}

Recall,
\begin{align}
    \begin{split}
        & \mathbf{F} = \sum_{i=1}^K\mathbf{F}_i\\
        & \mathbf{M}^C = \mathbf{M}_e+\sum_{i=1}^K(\mathbf{r}_i-\mathbf{r}_C)\times\mathbf{F}_i
    \end{split}
\end{align}
Hence, the mechanical power of the resultant forces and moments can be written as
\begin{align}
    \mathbf{F}\cdot\mathbf{v}_C+\mathbf{M}^C\cdot\bomega = \lp\sum_{i=1}^K\mathbf{F}_i\rp\cdot\mathbf{v}_C+\mathbf{M}_e\cdot\bomega+\lp \sum_{i=1}^K(\mathbf{x}_i-\mathbf{x}_C)\times\mathbf{F}_i\rp\cdot\bomega
\end{align}
With some minor manipulations involving the identity $\mathbf{a}\cdot(\mathbf{b}\times\mathbf{c}) = \mathbf{c}\cdot(\mathbf{a}\times\mathbf{b})$ and noting that $\mathbf{v}_i = \mathbf{v}_C+\bomega\times(\mathbf{x}_i-\mathbf{x}_C)$, we find that
\begin{align}
    \mathbf{F}\cdot\mathbf{v}_C+\mathbf{M}\cdot\bomega = \sum_{i=1}^K\mathbf{F}_i\cdot\mathbf{v}_i+\mathbf{M}_e\cdot\bomega.
\end{align}
In conclusion, we have an alternative form of the work-energy theorem that proves to be far easier to use in applications:
\begin{align}
    \dot{T} = \sum_{i=1}^K\mathbf{F}_i\cdot\mathbf{v}_i+\mathbf{M}_e\cdot\bomega.
\end{align}

\subsection{Example: A bar of length $\ell$ and mass $m$ pendulum fixed at its end}


\begin{figure}[h!]
    \centering
    \begin{tikzpicture}
        \draw[thick] (0,0) -- (3,-4);
        \fill[black] (0,0) circle (0.1);
        \draw[thick,->] (0,0) -- (0,-1) node[below] {$\mathbf{E}_x$};
        \draw[thick,->] (0,0) -- (1,0) node[right] {$\mathbf{E}_y$};
        \draw[thick,->,blue] (0,0) -- (0.6,-0.8) node[below] {$\mathbf{e}_x$};
        \draw[thick,->,blue] (0,0) -- (0.8,0.6) node[above] {$\mathbf{e}_y$};
    \end{tikzpicture}
\end{figure}


\begin{align}
    \begin{split}
        & U = -\frac{mg\ell}{2}\cos(\theta),\\
        & E = \frac{m\ell^2}{6}\dot{\theta}^2-\frac{mg\ell}{2}\cos(\theta) = const.
    \end{split}
\end{align}

\subsection{Example: What happens if you attach a dead mass at the end of the rod?}

\begin{figure}[h!]
    \centering
    \begin{tikzpicture}
        \draw[thick] (0,0) -- (3,-4);
        \fill[black] (0,0) circle (0.1);
        \draw[thick,->] (0,0) -- (0,-1) node[below] {$\mathbf{E}_x$};
        \draw[thick,->] (0,0) -- (1,0) node[right] {$\mathbf{E}_y$};
        \draw[thick,->,blue] (0,0) -- (0.6,-0.8) node[below] {$\mathbf{e}_x$};
        \draw[thick,->,blue] (0,0) -- (0.8,0.6) node[above] {$\mathbf{e}_y$};
        \fill[black] (3,-4) circle (0.4);
    \end{tikzpicture}
\end{figure}

\begin{align}
    & U = -\frac{mg\ell}{2}\cos(\theta)-m_e g\ell\cos(\theta)
\end{align}

\underline{What happens if there is a follower force applied at the end of the rod always perpendicular to it?}

\begin{figure}[h!]
    \centering
    \begin{tikzpicture}
        \draw[thick] (0,0) -- (3,-4);
        \fill[black] (0,0) circle (0.1);
        \draw[thick,->] (0,0) -- (0,-1) node[below] {$\mathbf{E}_x$};
        \draw[thick,->] (0,0) -- (1,0) node[right] {$\mathbf{E}_y$};
        \draw[thick,->,blue] (0,0) -- (0.6,-0.8) node[below] {$\mathbf{e}_x$};
        \draw[thick,->,blue] (0,0) -- (0.8,0.6) node[above] {$\mathbf{e}_y$};
        
        \draw[thick,->] (3,-4) -- (4.6,-2.8) node[above] {$\mathbf{P}$};
    \end{tikzpicture}
\end{figure}

$\mathbf{P}$ is actively changing depending on what the configuration is. 

This is a nonconservative system.

\section{Conservative Moments}

Moments can also be conservative.

A torsional spring is just a model for some kind of elasticity at the joint.

\begin{figure}[h!]
    \centering
    \begin{tikzpicture}
        % Parameters for the spiral
        \draw[domain=0:1000, samples=100, smooth, variable=\t] 
            plot ({0.0005*\t * cos(\t)}, {0.0005*\t * sin(\t)});
        \draw[thick] (0,0) -- (3,-4);
        \fill[black] (0,0) circle (0.1);
        \draw[thick,->] (0,0) -- (0,-1) node[below] {$\mathbf{E}_x$};
        \draw[thick,->] (0,0) -- (1,0) node[right] {$\mathbf{E}_y$};
        \draw[thick,->,blue] (0,0) -- (0.6,-0.8) node[below] {$\mathbf{e}_x$};
        \draw[thick,->,blue] (0,0) -- (0.8,0.6) node[above] {$\mathbf{e}_y$};
    \end{tikzpicture}
\end{figure}

The torsional spring with stiffness $K$ creates a couple
\begin{align}
    \mathbf{M}_S = K\theta(-\mathbf{E}_Z)
\end{align}
The power of the couple is
\begin{align}
    \mathbf{M}_S\cdot\bomega = -K\theta\dot{\theta} = -\frac{d}{dt}\lp K\frac{\dot{\theta}^2}{2} \rp.
\end{align}
Hence, $U = \frac{1}{2}K\dot{\theta}^2$ for the torsional spring.

A constant couple $\mathbf{M}_e = M_e\mathbf{E}_Z$ is also conservative. 
Its power is
\begin{align}
    \mathbf{M}_e\cdot\bomega = M_e\dot{\theta} = -\frac{d}{dt}(-M_e\theta).
\end{align}
Hence, $U = -M_e\theta$ for a constant couple.

Note. This only works for $\bomega = \omega\mathbf{E}_z$. Constant couples are not conservative in general.

\newpage
\section{Impact Problems / Center of Percussion}

For rigid bodies, impact ideas are the same as in systems of particles,

\underline{Example}

Consider a projectile fired at a pendulum in the horizontal plane (no gravity). What is $x$ so that the reaction for at the pin is almost zero during impact.

\begin{figure}[h!]
    \centering
    \begin{tikzpicture}
        \draw[thick] (0,0) -- (0,-4);
        \fill (0,0) circle (0.1);
        \fill (-2,-3) circle (0.25);
        \draw[thick,->] (-2,-3) -- (-1,-3) node[above] {$\mathbf{v}_0$};
        \draw[->] (1,0) -- (1,-3) node[midway, right] {$x$};
    \end{tikzpicture}
\end{figure}

If $x$ is big enough, the horizontal component of reaction force at $O$ will point to the left. If $x$ is small enough, the horizontal component of the reaction force at $O$ will point to the right. Somewhere in between, the reaction force at $O$ will be minimized. This optimal location $x$ is called the center of percussion.

